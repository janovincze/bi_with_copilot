# Copilot API Configuration
# The copilot-api server runs on this URL by default
COPILOT_API_URL=http://localhost:4141/v1

# Model to use (gpt-4.1 is the default for GitHub Copilot)
LLM_MODEL=gpt-4.1

# DuckDB database path
DB_PATH=../dbt_project/jaffle_shop.duckdb

# Optional: If using OpenAI directly instead of Copilot
# OPENAI_API_KEY=sk-...

# Optional: If using Ollama locally
# OLLAMA_HOST=http://localhost:11434
